import React, { useEffect, useState, useMemo } from "react";
import { DateTimeUtil } from "../DateTimeUtil";
const useDailyCard = (oneCall) => {
    const dateTimeUtil = useMemo(() => new DateTimeUtil(), []);
    

    useEffect(() => {

    });
    return {

    }
}

export default useDailyCard;